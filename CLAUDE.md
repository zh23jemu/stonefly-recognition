# Claude Code 执行规则（Windows / 少确认 / 安全优先）

## 1. 核心原则

始终优先选择安全、非破坏性、少用命令行的做法。

当前目录的实际工具权限由 `settings.json` 管理；本文件用于补充行为习惯和执行边界。若两者看起来有差异，以 `settings.json` 的允许和拒绝规则为硬性权限边界。

---

## 2. 文件操作

- 读取文件时，优先使用 Claude Code 内置读取能力。
- 避免为了读取文件而调用 shell 命令。
- 不要使用以下命令读取文件：
  - `cat`
  - `Get-Content`
  - `sed`
  - `awk`
- 编辑文件前必须先读取并理解原文件内容。
- 修改时保持最小变更，不做无关重构。

---

## 3. Shell 命令策略

尽量避免 shell 命令；只有在确实需要时才使用。

如果必须使用 shell，优先使用安全、只读或低风险命令：

- `pwd`
- `ls`
- `echo`
- `rg` / `grep`
- `git status`
- `git diff`
- `git log`
- `docker exec`
- `mysql`

禁止主动执行破坏性系统命令，尤其是：

- `rm` / `rm -rf`
- `del`
- `erase`
- `rd`
- `rmdir`
- `Remove-Item`
- `Clear-Content`
- `git reset`
- `git clean`
- `git restore`
- `git checkout`
- `format`
- `diskpart`
- 关机、重启或磁盘相关操作

这些高风险命令已经在 `settings.json` 的 `deny` 中尽量拦截；即使工具层没有拦住，也不要主动执行。

---

## 4. 删除策略（重要）

不要直接删除本地文件。

如果任务需要删除文件：

- 优先只给出建议删除命令，不执行。
- 或者说明可以移动到临时目录，让用户确认后再处理。
- 如果必须执行文件删除，必须先获得用户明确确认。

数据库操作不适用本地文件删除规则，但仍必须谨慎确认目标范围。

---

## 5. 数据库策略

可以执行数据库修改操作，包括删除记录和删除表。

执行前必须确认：

- 目标数据库
- 目标表或集合
- 条件范围
- 是否为生产环境或真实数据

涉及不可逆数据库操作时，先清楚说明影响范围，再执行用户明确要求的操作。

---

## 6. 代码编辑规则

- 编辑前先读取相关文件。
- 按现有项目结构和风格修改。
- 只改和任务直接相关的内容。
- 不主动重构无关代码。
- 不覆盖用户已有改动。
- 发现工作区已有未说明的变更时，默认认为是用户改动，并在其基础上继续。

## 代码注释策略

生成新代码时，默认使用较详细的中文注释。

注释要求：

- 对模块、类、函数、复杂逻辑块说明用途和关键思路。
- 对重要分支、参数、返回值、边界情况和非显而易见的实现细节写清楚原因。
- 不写无意义的逐行语法解释。
- 修改代码时同步更新相关注释。
- 在已有项目中尊重原项目风格，但除非用户明确要求，新增说明性注释优先使用中文。

---

## 7. `.gitignore` 策略

维护 Git 仓库时，要根据项目真实内容维护 `.gitignore`。

规则：

- 创建或更新 `.gitignore` 前，先检查项目语言、框架、构建工具、包管理器、测试工具、编辑器文件、系统文件、缓存、日志和本地环境文件。
- 如果项目需要但缺少 `.gitignore`，应创建。
- 当项目新增或移除语言、框架、构建产物、依赖目录、缓存、数据库、本地配置等内容时，应同步更新。
- 忽略生成文件、缓存、密钥、本地环境文件、虚拟环境、依赖目录、构建产物、覆盖率输出、编辑器文件和系统垃圾文件。
- 保留已有且合理的忽略规则，不随意删除。
- 不添加和当前项目无关的大型通用模板。

## Git 提交策略

如果用户要求"提交 git"或表达等价意图，默认协助完成从检查到推送的一整套 Git 提交流程。

规则：

- 提交前先根据当前项目内容和最近变更检查是否需要创建或更新 `.gitignore`；如有必要，先更新 `.gitignore` 再提交。
- 在不涉及用户明确排除文件的前提下，默认执行 `git add`，将本次应提交的相关修改加入暂存区。
- 默认自动执行 `git commit`。
- 提交信息必须使用中文，并符合 Conventional Commits 风格。
- 提交信息必须包含标题和内容，不得只写单行标题。
- 标题应概括本次提交的主要变更，内容应说明关键修改点、原因或影响范围。
- 完成提交后，默认自动执行 `git push`，将当前分支推送到对应远程。
- 如果仓库状态、远程分支状态或提交范围存在明显风险或歧义，再向用户确认；否则默认直接完成整个流程。

---

## 8. 图片与页面布局策略

生成或放置图片时，必须检查图片和文字不会互相遮挡。

规则：

- 最终交付前确认重要文字完整可见、可读。
- 不让图片遮住标题、正文、按钮、标签、说明文字或其他关键 UI。
- 不把文字放在复杂图片区域上，除非有足够对比度、内边距和背景处理。
- 如果出现重叠或拥挤，调整图片尺寸、位置、间距、容器布局或文字位置。
- 响应式页面需要同时检查桌面和移动端布局。

---

## 9. 自动化与少确认行为

当前 `settings.json` 使用 `acceptEdits`，目标是减少普通编辑和安全操作的确认次数。

在少确认模式下仍要遵守：

- 避免触发高风险权限。
- 优先使用内置工具。
- 优先做只读分析，再做最小编辑。
- 不执行删除、重置、清理、磁盘、关机重启等危险操作。
- 对外发送、上传、提交、发布、付款、账号权限变更等操作，仍需用户明确确认。

---

## 10. 首选工作流

1. 读取文件和上下文。
2. 分析现有逻辑与约束。
3. 说明准备进行的关键修改。
4. 应用安全、最小的编辑。
5. 做必要验证。
6. 用简体中文总结结果。

---

## 11. 语言策略

- 始终使用简体中文回复。
- 除非用户明确要求，不使用英文输出。
- 即使命令、工具或文档输出是英文，也要在最终说明中转换为中文。

---

## 12. Python 环境策略（严格）

始终使用项目本地虚拟环境 `.venv`。

规则：

- 不使用系统 Python 直接运行项目脚本。
- 不依赖激活虚拟环境。
- 始终显式调用虚拟环境中的解释器或 pip。

Windows 执行路径：

- Python：`.venv\Scripts\python.exe`
- pip：`.venv\Scripts\pip.exe`

Linux / macOS 执行路径：

- Python：`.venv/bin/python`
- pip：`.venv/bin/pip`

创建 `.venv` 前，先检查项目约束并选择合适 Python 版本：

1. `.python-version`、`.tool-versions`、`runtime.txt`、`pyproject.toml`、`Pipfile`、`setup.py`、`setup.cfg`
2. `tox.ini`、`noxfile.py`、`Dockerfile`、`.github/workflows`、`.gitlab-ci.yml`
3. `poetry.lock`、`pdm.lock`、`uv.lock`、`requirements*.txt`
4. README 或项目文档示例

选择规则：

- 优先使用项目明确要求的版本。
- 多个已安装版本都满足时，优先使用 CI 或项目工具中指定的版本。
- 没有明确约束时，选择最新稳定且兼容依赖的已安装版本。
- 创建虚拟环境时显式指定解释器，例如 Windows 使用 `py -3.11 -m venv .venv` 或具体 `python.exe` 路径。

禁止：

- `python xxx.py`
- `pip install xxx`

要求：

- `.venv\Scripts\python.exe xxx.py`
- `.venv\Scripts\pip.exe install xxx`

---

## 13. 敏感信息策略

- 不主动读取 `.env`、密钥、令牌、私钥、凭据、`secrets` 目录等敏感文件。
- 不把 API Key、密码、令牌或个人隐私信息提交到 Git。
- 当前 `settings.json` 中的敏感环境变量不应作为公开仓库内容传播。
- 如果用户要求处理敏感信息，必须只在用户明确授权的范围内进行。
